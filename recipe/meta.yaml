{% set name = "caped-ai-metrics" %}
{% set version = "1.1.8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/caped-ai-metrics-{{ version }}.tar.gz
  sha256: a46d0beaf203928c8c8324d8ccc0768af2cc8ddb1fe716eca3f7575ecf52c4bc

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.9
  run:
    - pandas
    - python >=3.9
    - scikit-image
    - scipy
    - seaborn
    - stardist

test:
  imports:
    - caped_ai_metrics
  commands:
    - pip list
    - pip check
  requires:
    - pip

about:
  home: https://github.com/Kapoorlabs-CAPED/caped-ai-metrics
  summary: Metric computation toolkit for segmentation and classification tasks
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - goanpeca
    - kapoorlab
